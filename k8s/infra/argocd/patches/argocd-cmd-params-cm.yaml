apiVersion: v1
kind: ConfigMap
metadata:
  name: argocd-cmd-params-cm
data:
  "server.insecure": "true"
  "server.basehref": "/"
  "server.rootpath": ""
  # Increase controller->repo-server timeout for slow GitHub fetches
  "controller.repo.server.timeout.seconds": "300"
